<%= form_for(@gazette) do |f| %>
  <% if @gazette.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gazette.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @gazette.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <legend>Enter gazette</legend>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name, size: 40 %>
    </div>
    <div class="field">
      <%= f.label :description %></br >
      <%= f.text_area :description %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </fieldset>
<% end %>