$(".meta-data").replaceWith("<%= j render partial: 'edit' %>");
document.onkeyup = null;

$(document).ready(function() {
  $(".edit-mode #off").removeClass('selected-item');
  $(".edit-mode #on").addClass('selected-item');
});
