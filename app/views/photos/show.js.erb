$(".meta-data").replaceWith("<%= j render partial: 'edit' %>");

$(document).ready(function() {
  $(".edit-mode #off").addClass('selected-item');
  $(".edit-mode #on").removeClass('selected-item');
});
