<h1>Listing place_types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @place_types.each do |place_type| %>
      <tr>
        <td><%= place_type.name %></td>
        <td><%= place_type.description %></td>
        <td><%= link_to 'Show', place_type %></td>
        <td><%= link_to 'Edit', edit_place_type_path(place_type) %></td>
        <td><%= link_to 'Destroy', place_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Place type', new_place_type_path %>
