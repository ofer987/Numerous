<%= form_for(@tag_link) do |f| %>
  <% if @tag_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_link.errors.count, "error") %> prohibited this tag_link from being saved:</h2>

      <ul>
      <% @tag_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :tagable_id %><br>
    <%= f.text_field :tagable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
